spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/spring-webservice
    username: sa
    password:
    driver-class-name: org.h2.Driver


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#  h2:
#    console:
#      enabled: true
  sql:
    init:
      data-locations: classpath:data-h2.sql
#  sql:
#    init:
#      data-locations: classpath:dxata-h2.sql